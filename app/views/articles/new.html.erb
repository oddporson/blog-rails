<h1>New Article</h1>

<%= form_for :article, url: articles_path do |f| %>

<%# we check if there's any errors with @article.errors.any?. In this case, we show a list of errors with %>
<%# The reason why we added @article = Article.new in the ArticlesController is that otherwise @article would be nil in our view, and calling @article.errors.any? would throw an error. %>
<% if @article.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%# pluralize is a rails helper that takes a number and a string as a its arguments. if number is greater than one, the string will automatically be pluralized. %>
        <%= pluralize(@article.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
<%= link_to 'Back', articles_path %>
